version: "3.6"

services:
  web:
    volumes:
      - web-node_modules:/app/web/node_modules
#  backend:
#    volumes:
#      - backend-node_modules:/opt/app/backend/node_modules

volumes:
  web-node_modules:
#  backend-node_modules:
